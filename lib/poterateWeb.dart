import 'package:flutter/material.dart';

class POterateWeb extends StatelessWidget {
  const POterateWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("PoterateWeb"),
        centerTitle: true,
        backgroundColor: Colors.blue,
      ),
      body: Center(child: Text("PoterateWeb"),),
    );
  }
}
